import React from 'react'
import ReactDOM from 'react-dom/client'
import { Lista } from './Lista_persona.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Lista/>
  </React.StrictMode>,
)
